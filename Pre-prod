Проведи финальный pre-production аудит проекта без большого рефакторинга.

Контекст:
- Build backend/frontend зелёный.
- 2FA управляется флагами:
  - LOGIN_2FA_ENABLED
  - CRITICAL_2FA_ENABLED
- Rate limiting уже внедрён.
- Sigur events используют пагинацию, truncatedDays оставлен deprecated, paginatedDays добавлен.
- Есть smoke-тесты на vitest + supertest.
- Chat заморожен.
- Нельзя делать rewrite.
- Нельзя менять unrelated code.

Что нужно сделать:
1. Проверь, что документация соответствует коду:
   - FINAL_TECH_STATUS.md
   - SIGUR_IMPORT_NOTES.md
   - NEXT_BACKLOG.md
2. Проверь production-readiness по пунктам:
   - auth/2FA flags
   - rate limiting
   - trust proxy / корректность client IP для лимитов
   - protected critical routes
   - smoke tests
3. Проверь, нет ли несостыковки между:
   - env flags
   - middleware behavior
   - docs
4. Составь короткий pre-production checklist:
   - что включить в env
   - что проверить руками
   - что проверить в логах после деплоя
5. Не вноси новый большой рефакторинг.
6. Менять код только если найдена явная несостыковка production-readiness.

Формат ответа:
1. Production readiness status
2. Что готово
3. Что требует ручной проверки
4. Какие env обязательны
5. Какие логи/метрики смотреть после запуска
6. Если есть небольшой точечный blocker — перечисли его отдельно